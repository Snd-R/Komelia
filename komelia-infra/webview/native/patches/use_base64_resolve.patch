diff --git a/core/include/webview/detail/engine_base.hh b/core/include/webview/detail/engine_base.hh
index 01c8d29..b1e0e65 100644
--- a/core/include/webview/detail/engine_base.hh
+++ b/core/include/webview/detail/engine_base.hh
@@ -126,7 +126,7 @@ window.__webview__.onUnbind(" +
                            escaped_result + ")";
           eval(js);
         },
-        result.empty() ? "undefined" : json_escape(result)));
+        result.empty() ? "undefined" : '"' + result + '"'));
   }
 
   result<void *> window() { return window_impl(); }
@@ -219,6 +219,10 @@ protected:
       return ((s.length % 2) == 1 ? '0' : '') + s;\n\
     }).join('');\n\
   }\n\
+  function base64ToBytes(base64) {\n\
+    const binString = atob(base64);\n\
+    return Uint8Array.from(binString, (m) => m.codePointAt(0));\n\
+  }\n\
   var Webview = (function() {\n\
     var _promises = {};\n\
     function Webview_() {}\n\
@@ -243,7 +247,8 @@ protected:
       var promise = _promises[id];\n\
       if (result !== undefined) {\n\
         try {\n\
-          result = JSON.parse(result);\n\
+          const jsonString = new TextDecoder().decode(base64ToBytes(result)); \n\
+          result = JSON.parse(jsonString);\n\
         } catch (e) {\n\
           promise.reject(new Error(\"Failed to parse binding result as JSON\"));\n\
           return;\n\
